// describe('stuff', () => {
//   it('should work', () => {
//     expect(1).toBe(1)
//   })
// })

// import {DeviceSchema} from '../src'

// describe('device schema', () => {
//   it('should fail if name is missing', () => {
//     const payload = {}
//     return DeviceSchema.validate(payload).catch(e => {
//       expect(e).toMatch('xx')
//     })
//   })
// })
